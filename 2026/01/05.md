# 2026/01/05

## Accomplishments
- Bikin kerangka alur untuk ngerjain submission kelas Dicoding Deep Learning Tingkat Mahir:
    - KRITERIA 1
        - load dataset
        - EDA
            - bikin heatmap korelasi antar fitur yang dipilih (minimal 3 fitur)
            - analisis dekomposisi pada data target dan plot hasil analisisnya
            - uji ACF dan PACF untuk menentukan windows size
        - feature engineering dengan rolling statistics (minimal 1 fitur)
        - bagi data jadi data train-val-test
        - normalisasi / standardisasi masing-masing fitur secara terpisah
        - bikin pipeline data dengan tf.data.Dataset 
        - bangun baseline LSTM dan latih pakai model.fit()
    - KRITERIA 2
        - bikin satu layer dense custom dari nol
        - bikin satu layer multi head attention custom dari nol
        - bikin satu custom layer, bebas. contoh: dropout layer, normalization layer, activation function layer (elu, leaky relu, dll)
        - bangun arsitektur model seq2seq lstm untuk multi-step time series forecasting sebanyak 24 step dengan pendekatan teacher forcing menggunakan:
            - functional api
            - model subclassing
        - pastiin lstm model juga ditujukan untuk multi-step time series forecasting sebanyak 24 step (di kriteria 1)
    - KRITERIA 3
        - bikin custom loss pakai mae, dan harus bisa nambahin weight loss pada step yang lebih jauh
        - bikin custom callback early stopping yang mengurangi learning rate secara bertahap saat validation loss nya stagnan selama beberapa epoch
        - bangun custom training yang memanfaatkan tf.GradientTape beserta custom loss dan custom callback yang udah dibangun sebelumnya
        - jalankan loop training (custom training) untuk melatih custom model yang udah dibangun di Kriteria 2. pastikan selama prosesnya, jumlah epoch, loss, dan val loss nya tertampilkan di output code. 
        - pastiin performa model custom seq2seq lstm saat dievaluasi pada data test di bawah 0.015 mae (kondisi sebelum di-inverse scaled)
        - inference prediksi dengan teknik autoregressive untuk model seq2seq, dan inference biasa untuk model lstm biasa
        - visualisasikan perbandingan data aktual dan hasil prediksi dengan:
            - line chart
            - tabel (kolom: jam ke, data aktual, hasil prediksi, selisih)

- Bikin kerangkanya di notebook.
- Ngisi baris kode sampai baseline model lstm. Tapi belum sampai fit model.


## Thoughts
- Kriteria 1 hampir selesai. Rada lama proses pelatihan model baseline nya. Mungkin kurang simpel. Besok kucoba persimpel lagi. 

## Next Steps
- Lanjut ngerjain notebook
